# Don't ship source — only the compiled build/
src/
tsconfig.json

# v2 proxy has zero runtime dependencies (node:readline + global fetch only).
# Exclude node_modules entirely — keeps the .mcpb bundle tiny.
node_modules/

# Dev-only
.git/
.gitignore
.github/
.vscode/
*.log
*.tgz
*.mcpb
package-lock.json

# OS junk
.DS_Store
